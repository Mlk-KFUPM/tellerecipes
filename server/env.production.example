# Production Environment Variables
# Rename this file to .env in your production environment

# Server Configuration
NODE_ENV=production
PORT=5001

# Database
# REQUIRED: Your MongoDB connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>

# Security Secrets
# REQUIRED: Generate strong random strings for these (e.g., `openssl rand -hex 64`)
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=

# CORS Configuration
# REQUIRED: Comma-separated list of your frontend URLs
CLIENT_ORIGINS=https://your-app.com,https://www.your-app.com
